# Required for server-side NVIDIA Nemotron analysis (never NEXT_PUBLIC_).
NVIDIA_API_KEY=
# Optional model override.
NVIDIA_MODEL=nvidia/nemotron-3-super-120b-a12b

# Only needed locally to regenerate the included voice clips.
# Not required on Vercel: playback serves the saved MP3s in public/audio.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=JBFqnCBsd6RMkjVDRZzb
